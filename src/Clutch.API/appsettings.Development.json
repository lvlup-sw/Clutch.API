{
  "ConnectionStrings": {
    // Change this connection string to what is provisioned in init.sh
    "containerImageDb": "Host=localhost;Database=containerImageDb;",
    "Redis": "redis:6379,abortConnect=false,ssl=false,allowedAdmin=true"
  }
}